## Summary
- What changed and why (1-3 bullets)

## Test plan
Paste the actual command(s) you ran and their output/result. "Wrote tests" is not
evidence — the output below is.

```
command:
result:
```

Manual verification steps (if applicable):
- Step taken:
- Observed result:

- [ ] Tests pass locally (`go build ./...`, `go vet ./...`, `go test ./...`, or repo equivalent)

## Testing evidence honesty
- Verified:
- Could NOT verify (and why — e.g. no access to hardware/environment):
